// Basic Hyperlambda example demonstrating core syntax
.data
   item1:Hello from Item1
   item2:Hello from Item2
   item3:Hello from Item3

// For-each loop example
for-each:x:@.data/*
   log.info:x:@.dp/#

// Variable declarations with types
.foo1:int:5
.foo2:long:55
.foo3:string:Howdy world
.foo4:bool:true

// Conditional statement
if:x:@.foo4
   log.info:The boolean is true
else
   log.info:The boolean is false

