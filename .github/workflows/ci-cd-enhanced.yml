name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, 'copilot/**' ]
  pull_request:
    branches: [ main, develop ]
  release:
    types: [ published ]
  workflow_dispatch:
    inputs:
      create_stable_release:
        description: 'Create v1.0.0 stable release'
        required: false
        default: 'false'
        type: choice
        options:
        - 'false'
        - 'true'

permissions:
  contents: write
  packages: write
  actions: read
  checks: write
  pull-requests: write
  security-events: write
  statuses: write

env:
  DOTNET_VERSION: '8.0.x'
  SOLUTION_PATH: 'src/Minotaur.sln'
  PROJECT_PATH: 'src/Minotaur/Minotaur.csproj'

jobs:
  calculate-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.final-version.outputs.version }}
      prerelease: ${{ steps.final-version.outputs.prerelease }}
      full-version: ${{ steps.final-version.outputs.full-version }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install GitVersion
      uses: gittools/actions/gitversion/setup@v0
      with:
        versionSpec: '5.x'

    - name: Determine Version
      id: gitversion
      uses: gittools/actions/gitversion/execute@v0
      with:
        useConfigFile: true

    - name: Adjust version for pull requests
      id: final-version
      run: |
        VERSION="${{ steps.gitversion.outputs.SemVer }}"
        PRERELEASE="${{ steps.gitversion.outputs.PreReleaseTag }}"
        # Use SemVer (without build metadata) for package version to ensure NuGet.org compatibility
        # FullSemVer includes build metadata like +37 which causes issues on NuGet.org
        FULL_VERSION="${VERSION}"
        
        # Check if this is a pull request and adjust version accordingly
        if [[ "${{ github.event_name }}" == "pull_request" ]]; then
          if [[ "${{ github.base_ref }}" == "main" ]]; then
            # Pull request to main -> use beta
            if [[ "$PRERELEASE" == "PullRequest" ]]; then
              PRERELEASE="beta"
              FULL_VERSION="${VERSION}-beta.PR${{ github.event.number }}"
            fi
          else
            # Pull request to other branches -> use alpha
            if [[ "$PRERELEASE" == "PullRequest" ]]; then
              PRERELEASE="alpha"
              FULL_VERSION="${VERSION}-alpha.PR${{ github.event.number }}"
            fi
          fi
        fi
        
        echo "version=${VERSION}" >> $GITHUB_OUTPUT
        echo "prerelease=${PRERELEASE}" >> $GITHUB_OUTPUT
        echo "full-version=${FULL_VERSION}" >> $GITHUB_OUTPUT

    - name: Display Version
      run: |
        echo "Version: ${{ steps.final-version.outputs.version }}"
        echo "PreReleaseTag: ${{ steps.final-version.outputs.prerelease }}"
        echo "FullSemVer: ${{ steps.final-version.outputs.full-version }}"

  build-and-test:
    runs-on: ubuntu-latest
    needs: calculate-version
    outputs:
      test-results: ${{ steps.test.outputs.test-results }}
      coverage-results: ${{ steps.test.outputs.coverage-results }}
    env:
      NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: |
        # Retry: GitHub Packages restore intermittently fails (NU1301) on Windows runners
        dotnet restore ${{ env.SOLUTION_PATH }} || (sleep 20 && dotnet restore ${{ env.SOLUTION_PATH }}) || (sleep 40 && dotnet restore ${{ env.SOLUTION_PATH }})

    - name: Build solution
      run: |
        dotnet build ${{ env.SOLUTION_PATH }} \
          --configuration Release \
          --no-restore \
          -p:Version=${{ needs.calculate-version.outputs.version }} \
          -p:PackageVersion=${{ needs.calculate-version.outputs.full-version }}

    - name: Run tests with coverage
      id: test
      run: |
        dotnet test ${{ env.SOLUTION_PATH }} \
          --configuration Release \
          --no-build \
          --collect:"XPlat Code Coverage" \
          --results-directory ./TestResults/ \
          --logger "trx;LogFileName=test-results.trx"

    - name: Generate coverage report
      if: always()
      continue-on-error: true
      uses: danielpalme/ReportGenerator-GitHub-Action@5.4.1
      with:
        reports: './TestResults/**/coverage.cobertura.xml'
        targetdir: './CoverageReport'
        reporttypes: 'Html;JsonSummary'

    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: |
          TestResults/**/*.xml
          TestResults/**/*.trx

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-report
        path: CoverageReport/

  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    needs: [calculate-version, build-and-test]
    env:
      NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Shallow clones should be disabled for better analysis
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Restore dependencies
      run: |
        # Retry: GitHub Packages restore intermittently fails (NU1301) on Windows runners
        dotnet restore ${{ env.SOLUTION_PATH }} || (sleep 20 && dotnet restore ${{ env.SOLUTION_PATH }}) || (sleep 40 && dotnet restore ${{ env.SOLUTION_PATH }})
    
    - name: Build for analysis
      run: |
        dotnet build ${{ env.SOLUTION_PATH }} \
          --no-restore \
          --configuration Release \
          -p:Version=${{ needs.calculate-version.outputs.version }} \
          -p:PackageVersion=${{ needs.calculate-version.outputs.full-version }}
    
    - name: Run security analysis
      run: |
        dotnet list ${{ env.SOLUTION_PATH }} package --vulnerable --include-transitive || true
    
    - name: Check for outdated packages
      run: |
        dotnet list ${{ env.SOLUTION_PATH }} package --outdated || true

  lint-and-format:
    name: Lint and Format Check
    runs-on: ubuntu-latest
    needs: calculate-version
    env:
      NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Install dotnet format tool
      run: dotnet tool install -g dotnet-format
    
    - name: Restore dependencies
      run: |
        # Retry: GitHub Packages restore intermittently fails (NU1301) on Windows runners
        dotnet restore ${{ env.SOLUTION_PATH }} || (sleep 20 && dotnet restore ${{ env.SOLUTION_PATH }}) || (sleep 40 && dotnet restore ${{ env.SOLUTION_PATH }})
    
    - name: Check code formatting
      run: dotnet format ${{ env.SOLUTION_PATH }} --verify-no-changes --verbosity diagnostic --exclude examples/
    
    - name: Run static analysis
      run: |
        # --no-restore: use the retried restore above; an implicit restore here can hit
        # transient GitHub Packages issues whose NU warnings become errors under TreatWarningsAsErrors
        dotnet build ${{ env.SOLUTION_PATH }} \
          --no-restore \
          --configuration Release \
          --verbosity normal \
          -p:TreatWarningsAsErrors=true \
          -p:Version=${{ needs.calculate-version.outputs.version }} \
          -p:PackageVersion=${{ needs.calculate-version.outputs.full-version }}

  performance-test:
    name: Performance Tests
    runs-on: ubuntu-latest
    needs: [calculate-version, build-and-test]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    env:
      NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
    
    - name: Restore dependencies
      run: |
        # Retry: GitHub Packages restore intermittently fails (NU1301) on Windows runners
        dotnet restore ${{ env.SOLUTION_PATH }} || (sleep 20 && dotnet restore ${{ env.SOLUTION_PATH }}) || (sleep 40 && dotnet restore ${{ env.SOLUTION_PATH }})
    
    - name: Build solution
      run: |
        dotnet build ${{ env.SOLUTION_PATH }} \
          --no-restore \
          --configuration Release \
          -p:Version=${{ needs.calculate-version.outputs.version }} \
          -p:PackageVersion=${{ needs.calculate-version.outputs.full-version }}
    
    - name: Run demo application for performance testing
      run: |
        if [ -d "src/Minotaur.Demo" ]; then
          cd src/Minotaur.Demo
          timeout 30s dotnet run --configuration Release || echo "Demo completed or timed out as expected"
        else
          echo "No demo projects found for performance testing"
        fi

  package:
    runs-on: ubuntu-latest
    needs: [calculate-version, build-and-test, code-quality, lint-and-format]
    outputs:
      package-path: ${{ steps.package.outputs.package-path }}
    env:
      NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: |
        # Retry: GitHub Packages restore intermittently fails (NU1301) on Windows runners
        dotnet restore ${{ env.SOLUTION_PATH }} || (sleep 20 && dotnet restore ${{ env.SOLUTION_PATH }}) || (sleep 40 && dotnet restore ${{ env.SOLUTION_PATH }})

    - name: Build for packaging
      run: |
        dotnet build ${{ env.SOLUTION_PATH }} \
          --configuration Release \
          --no-restore \
          -p:Version=${{ needs.calculate-version.outputs.version }} \
          -p:PackageVersion=${{ needs.calculate-version.outputs.full-version }}

    - name: Create NuGet package
      id: package
      run: |
        dotnet pack ${{ env.PROJECT_PATH }} \
          --configuration Release \
          --no-build \
          --output ./packages \
          -p:PackageVersion=${{ needs.calculate-version.outputs.full-version }}
        echo "package-path=./packages" >> $GITHUB_OUTPUT

    - name: Upload packages as artifacts
      uses: actions/upload-artifact@v4
      with:
        name: nuget-packages
        path: ./packages/*.nupkg

  publish-pr-package:
    runs-on: ubuntu-latest
    needs: [calculate-version, build-and-test, package]
    if: github.event_name == 'pull_request'
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download packages
      uses: actions/download-artifact@v4
      with:
        name: nuget-packages
        path: ./packages

    - name: Publish to GitHub Packages
      run: |
        dotnet nuget push ./packages/*.nupkg \
          --api-key ${{ secrets.GITHUB_TOKEN }} \
          --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json \
          --skip-duplicate
      env:
        NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  prerelease:
    runs-on: ubuntu-latest
    needs: [calculate-version, build-and-test, package]
    if: |
      github.event_name == 'push' && 
      (contains(github.ref, 'refs/heads/develop') || 
       contains(github.ref, 'refs/heads/copilot/') ||
       contains(github.ref, 'refs/heads/feature/'))
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download packages
      uses: actions/download-artifact@v4
      with:
        name: nuget-packages
        path: ./packages

    - name: Create Pre-release
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ needs.calculate-version.outputs.full-version }}
        name: Minotaur v${{ needs.calculate-version.outputs.full-version }}
        body: |
          ## Minotaur Pre-release v${{ needs.calculate-version.outputs.full-version }}
          
          This is a pre-release version generated from branch: ${{ github.ref_name }}
          
          ### Changes
          - Enhanced location tracking precision APIs
          - Context modifier API exposure  
          - Rule activation callback implementation
          - Complete GrammarForge requirements implementation
          
          ### Installation
          ```bash
          dotnet add package DevelApp.Minotaur --version ${{ needs.calculate-version.outputs.full-version }}
          ```
          
          **Note**: This is a pre-release version intended for testing.
        draft: false
        prerelease: true
        files: ./packages/*.nupkg

  release:
    runs-on: ubuntu-latest
    needs: [calculate-version, build-and-test, package]
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.calculate-version.outputs.prerelease == '') ||
      (github.event_name == 'release' && github.event.action == 'published') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.create_stable_release == 'true')
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Download packages
      uses: actions/download-artifact@v4
      with:
        name: nuget-packages
        path: ./packages

    - name: Create GitHub Release (for main branch pushes and manual dispatch)
      if
Ú]X][Û[YHOH	Ü\Ú	È	Ú]XYOH	ÜYËÚXYËÛXZ[ÊH
Ú]X][Û[YHOH	ÝÛÜÙÝ×Ù\Ü]Ú	È	Ú]X][[]ËÜX]WÜÝXWÜ[X\ÙHOH	ÝYIÊB\Ù\ÎÛÙÜËØXÝ[ÛYÚ\[X\ÙPB[ÒUPÕÒÑS	ÞÈÙXÜ]ËÒUPÕÒÑS_BÚ]Y×Û[YNÞÈÚ]X][Û[YHOH	ÝÛÜÙÝ×Ù\Ü]Ú	È		ÌK	ÈYYËØ[Ý[]K]\Ú[ÛÝ]]Ë\Ú[Û_B[YNZ[Ý]\ÞÈÚ]X][Û[YHOH	ÝÛÜÙÝ×Ù\Ü]Ú	È		ÌK	ÈYYËØ[Ý[]K]\Ú[ÛÝ]]Ë\Ú[Û_HHÝXH[X\ÙHÙNÈZ[Ý]\ÞÈYYËØ[Ý[]K]\Ú[ÛÝ]]Ë\Ú[Û_HHÝXH[X\ÙBÈÈ<'ã¢HÚ]	ÜÈ]Â\È\ÈHÝXH[X\ÙHÙZ[Ý]\HÝÙ\[ÛÙÛ]]HÜ\Y][È[\Ú[È[Y]ÛÜËÈÈÈ8§!HÛÜHX]\\ÂH
Ý\\Ù\[YÜ][ÛÛÛ\]H\Ú[È\[[HÚ]Ý\XK\Ý\XYÙÚ[ÈØ\X[]Y\ÂH
Ü\Y]Ü[ÛÙÛ]]HÜ\Y][ÈÚ]ÛÛ\Z[Ú]H[ËÜYÈÝ\ÜH
YÚ[Þ\Ý[J^[ÚXH[\Ú[ÈÞ\Ý[HÝ\Ü[ÈÈË]TØÜ\[]ÛH
[[ÙYØØ][ÛXÚÚ[ÊXÚ\ÙHT\ÈÜÞ[^YH]YØ][Û[X[\[][ÛH
ÛÛ^P]Ø\HY][ÊY[ÙYÛÛ^X]Ø\HY][ÈØ\X[]Y\ÈÜ[[YÙ[ÛÙHÙ[\][ÛÈÈÈ8§!H][Ü\^\Y[ÙHH
ÛÛ\Z[Ú]H\Ý[ÊMÈ[]\ÝÈÛÝ\[È[[Ý[Û[]HÚ]L	H\ÜÈ]BH
ÒKÐÑ\[[J[H]]ÛX]YZ[Ë\Ý[Ë]X[]HÚXÚÜË[[X\Ù\ÂH
XÚØÝ[Y[][ÛÛÛ\]HTHØÝ[Y[][ÛÚ]^[\\È[[YÜ][ÛÝZY\ÂH
YÚ\ÜX[ÙJÜ[Z^Y\Ú[È[ÛÜ]\È[YXÚY[Ü\Ü\][ÛÂÈÈÈ<'äé[Ý[][Û\ÚÝ]YXÚØYÙH][\Z[Ý]\K]\Ú[Û	ÞÈÚ]X][Û[YHOH	ÝÛÜÙÝ×Ù\Ü]Ú	È		ÌK	ÈYYËØ[Ý[]K]\Ú[ÛÝ]]Ë\Ú[Û_BÈÈÈ<'å)È\]Z\[Y[ÂHUÜ]\HÛÛ\]XHÚ]Ú[ÝÜËXSÔË[[^\È[X\ÙH\\Ù[ÈHÝXKÙXÝ[Û\XYH\Ú[ÛÙZ[Ý]\ÝZ]XHÜ[YÜ][Û[È][ÜY[ÛÜÙÝÜÈ[ÛÛÚZ[ËY[ÙB\[X\ÙN[ÙB[\ÎÜXÚØYÙ\ËÊ\ÙÂH[YNX\ÚÈÚ]XXÚØYÙ\Â[XÚÈX\Ú[ÈÈÚ]XXÚØYÙ\ËÝ]YÙ]\ÚÜXÚØYÙ\ËÊ\ÙÈKX\KZÙ^H	ÞÈÙXÜ]ËÒUPÕÒÑS_HK\ÛÝ\ÙHÎËÛYÙ]ÙËÚ]XÛÛKÉÞÈÚ]X\ÜÚ]ÜWÛÝÛ\_KÚ[^ÛÛK\ÚÚ\Y\XØ]B[QÑUÐUUÕÒÑS	ÞÈÙXÜ]ËÒUPÕÒÑS_BÈX\Ú[È\Ù]\ÈÚ]XXÚØYÙ\ÈÓH
ÍÌN][\ÛÛÝ[Y\È
KËÛÛ[JBÈ]][XØ]HÈÎËÛYÙ]ÙËÚ]XÛÛKÑ][\XZKÚ[^ÛÛÈ\H\È[[[Û[HÈQÙ]ÜÈX\ÚÝ\ÈX[X[ØÈÜX]HK[X\ÙHÛHXZ[[ÚÜX]K\ÝXK\[X\ÙN[Ë[ÛX[K[]\ÝYYÎØØ[Ý[]K]\Ú[ÛZ[X[]\ÝÛÙK\]X[]K[X[YÜX]BYÚ]X][Û[YHOH	ÝÛÜÙÝ×Ù\Ü]Ú	È	Ú]X][[]ËÜX]WÜÝXWÜ[X\ÙHOH	ÝYIÂ[QÑUÐUUÕÒÑS	ÞÈÙXÜ]ËÒUPÕÒÑS_BÝ\ÎH[YNÚXÚÛÝ]ÛÙB\Ù\ÎXÝ[ÛËØÚXÚÛÝ]H[YNÙ]\U\Ù\ÎXÝ[ÛËÜÙ]\YÝ]Ú]Ý]]\Ú[Û	ÞÈ[ÕUÕTÒSÓ_BH[YNZ[[XÚØYÙHK[Ý]\ÝÜH	ÞÈ[ÓÓUSÓÔU_BÝ]Z[	ÞÈ[ÓÓUSÓÔU_HKXÛÛYÝ\][Û[X\ÙHK[Ë\\ÝÜH\\Ú[ÛLK\XÚØYÙU\Ú[ÛLKÝ]XÚÈ	ÞÈ[ÒPÕÔU_HKXÛÛYÝ\][Û[X\ÙHK[ËXZ[K[Ý]]ÜXÚØYÙ\È\XÚØYÙU\Ú[ÛLKH[YNÜX]HK[X\ÙB\Ù\ÎÛÙÜËØXÝ[ÛYÚ\[X\ÙPBÚ]Y×Û[YNK[YNZ[Ý]\KH\ÝÝXH[X\ÙHÙNÈZ[Ý]\KH\ÝÝXH[X\ÙBÈÊ<'ã¢HÚ]	ÜÈ]Â\È\ÈH\ÝÝXH[X\ÙHÙZ[Ý]\HÝÙ\[ÛÙÛ]]HÜ\Y][È[\Ú[È[Y]ÛÜËÈÈÈ8§!HÛÜHX]\\ÂH
Ý\\Ù\[YÜ][ÛÛÛ\]H\Ú[È\[[HÚ]Ý\XK\Ý\XYÙÚ[ÈØ\X[]Y\ÂH
Ü\Y]Ü[ÛÙÛ]]HÜ\Y][ÈÚ]ÛÛ\Z[Ú]H[ËÜYÈÝ\ÜH
YÚ[Þ\Ý[J^[ÚXH[\Ú[ÈÞ\Ý[HÝ\Ü[ÈÈË]TØÜ\[]ÛH
[[ÙYØØ][ÛXÚÚ[ÊXÚ\ÙHT\ÈÜÞ[^YH]YØ][Û[X[\[][ÛH
ÛÛ^P]Ø\HY][ÊY[ÙYÛÛ^X]Ø\HY][ÈØ\X[]Y\ÈÜ[[YÙ[ÛÙHÙ[\][ÛÈÌâ Developer Experience  
          - **Comprehensive Testing**: 157 unit tests covering all functionality with 100% pass rate
          - **CI/CD Pipeline**: Fully automated builds, testing, quality checks, and releases
          - **Rich Documentation**: Complete API documentation with examples and integration guides
          - **High Performance**: Optimized parsing algorithms and efficient graph operations

          ### ð¦ Installation

          ```bash
          dotnet add package DevelApp.Minotaur --version 1.0.0
          ```(
          ### ð§ Requirements
          - .NET 8.0 or later
          - Compatible with Windows, macOS, and Linux

          This release represents a stable, production-ready version of Minotaur suitable for integration into development workflows and toolchains.
        draft: false
        prerelease: false
        files: ./packages/*.nupkg
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Publish v1.0.0 to GitHub Packages
      run: |
          echo "Publishing v1.0.0 to GitHub Packages..."
          dotnet nuget push ./packages/*.nupkg \
            --api-key ${{ secrets.GITHUB_TOKEN }} \
            --source https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json \
            --skip-duplicate
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}


  cleanup:
    runs-on: ubuntu-latest
    needs: [build-and-test, package]
    if: always()
    steps:
    - name: Cleanup artifacts (if needed)
      run: echo "Pipeline completed. Artifacts preserved for download."